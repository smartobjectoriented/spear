# A TESTED embedding stack — the one the equivalence gate was run against.
#
# This is not the required stack and not a lockfile for the project. It is a
# record: on one GPU host, these exact versions produced vectors BIT-IDENTICAL
# to the worker they replaced, for documents and for queries, on CPU/fp32 and
# on CUDA/fp16 alike (max absolute difference 0.0). Reproduce it when you want
# that result rather than merely a working embedder; otherwise install
# requirements.txt unpinned and let the resolver suit your host.
#
# ── the part that is not on PyPI ─────────────────────────────────────
# `torch==2.12.0+cu130` is a PyTorch CUDA-13 build. It does NOT exist on the
# normal PyPI index, and `pip install -c` against PyPI alone will fail to find
# it. It comes from PyTorch's own wheel index, which must be named explicitly:
#
#     https://download.pytorch.org/whl/cu130
#
# So the install is two steps, torch first from its own index:
#
#     python3 -m venv venv
#     venv/bin/pip install --upgrade pip
#     venv/bin/pip install \
#         --index-url https://download.pytorch.org/whl/cu130 \
#         --extra-index-url https://pypi.org/simple \
#         "torch==2.12.0+cu130"
#     venv/bin/pip install -r requirements.txt -c constraints-reds-tested.txt
#
# The nvidia-* pins below are torch's own dependencies at that version; they
# resolve from the same index. Nothing here names a host, an account, a card
# or a filesystem path -- this file describes software, not a machine.
#
# Python 3.12.3.  Recorded 2026-09-14.

sentence-transformers==5.5.1
torch==2.12.0+cu130
transformers==5.9.0
numpy==2.4.6

# resolved alongside them, recorded so the set is reproducible as a whole
certifi==2026.7.22
filelock==3.32.6
fsspec==2026.7.0
hf-xet==1.6.0
huggingface_hub==1.31.0
idna==3.19
Jinja2==3.1.6
joblib==1.6.0
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
packaging==26.3
PyYAML==6.0.3
regex==2026.9.10
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1
setuptools==81.0.0
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.22.2
tqdm==4.70.1
triton==3.7.0
typing_extensions==4.16.0

# torch's CUDA-13 runtime, from the same PyTorch index
nvidia-cublas==13.1.1.3
nvidia-cuda-cupti==13.0.85
nvidia-cuda-nvrtc==13.0.88
nvidia-cuda-runtime==13.0.96
nvidia-cudnn-cu13==9.20.0.48
nvidia-cufft==12.0.0.61
nvidia-cufile==1.15.1.6
nvidia-curand==10.4.0.35
nvidia-cusolver==12.0.4.66
nvidia-cusparse==12.6.3.3
nvidia-cusparselt-cu13==0.8.1
nvidia-nccl-cu13==2.29.7
nvidia-nvjitlink==13.0.88
nvidia-nvshmem-cu13==3.4.5
nvidia-nvtx==13.0.85
