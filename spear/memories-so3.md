- [2026-06-15] La souris SO3 sous QEMU est un pointeur ABSOLU `so3,absmouse` (device MMIO @0x08803000, regs X/Y/BTN/MAX, /dev/mouse, devices/input/absmouse.c), pas la souris PL050 relative — mapping 1:1 hôte/invité, sans grab. Côté QEMU: handler d'input absolu enregistré + qemu_input_handler_activate(). Le nœud dts kmi souris (PL050) est status="disabled".
- [2026-06-15] Ctrl-C sur une app multi-thread (LVGL/slv a un pthread tick): correctif en 3 parties — ipc/signal.c (sig_check) diffère le signal fatal au main thread; kernel/thread.c fait complete(threads_active) pour le dernier thread spawné même sans joiner; kernel/schedule.c (CONFIG_SCHED_RR, next_thread) ordonnance aussi un thread killed ou le main_thread d'un pcb ZOMBIE. fg_pcb (kernel/process.c, posé par sys_do_wait4) = job au premier plan; pl011.c envoie SIGINT à fg_pcb sinon annule la ligne via serial_intr.
- [2026-06-15] IRQ PL050 clavier/souris sur la machine QEMU virt: dts = SPI 11 (clavier) / 12 (souris), PAS 36/37 (vexpress) — doit matcher l'irqmap du patch QEMU hw/arm/virt.c.
- [2026-06-15] L'affichage SO3 est le PL111 CLCD (devices/fb/pl111.c, 1024x768x32, VRAM guest-phys 0x30000000, /dev/fb avec ioctls IOCTL_FB_HRES=1/VRES=2/SIZE=3/IS_REAL=4/BPP=5, mmap non-cacheable). usr/src/fb_test.c = exemple minimal. Lancer QEMU avec -display gtk (SDL ne présente pas la surface PL111).
- [2026-06-15] arm32: MUSL appelle fcntl64 (syscall NR 221) au lieu de fcntl; routé vers le handler fcntl via une ligne dans so3/syscall.tbl.
- [2026-06-15] Le noyau SO3 est construit IN-TREE et bitbake ne suit pas so3/so3/so3.bin: après `build.sh -k so3` il FAUT `deploy.sh -k bsp-so3` (ou -a) pour régénérer l'ITB+sdcard, sinon QEMU démarre l'ancien noyau.
- [2026-06-15] `build.sh -a bsp-so3` est NON privilégié (do_build = compilation pure + rootfs.fat vide via mkfs.vfat sur fichier). Le montage rootfs privilégié (losetup/mount) est dans usr-so3:do_deploy, tiré par deploy.sh. Seul bsp-linux câble do_itb avant do_build et a donc besoin de root au build.
- [2026-06-15] Composants fetchés (u-boot, qemu, avz): éditer la source dans le working tree puis `scripts/updiff.sh <recipe>` régénère les patches files/000N-*.patch (build/meta/classes/patch.bbclass, diff de ${S}.pristine vs IB_TARGET). IB_UPDIFF_EXCLUDE garde les arbres générés hors du patchset (qemu = "build subprojects GNUmakefile"). Le noyau SO3 et usr sont versionnés → pas de patches.
- [2026-06-15] Affichage graphique = `scripts/stg.sh` (GTK); il exporte GDK_BACKEND=x11 car sous Wayland HiDPI le mapping du pointeur absolu sort décalé (XWayland corrige). `scripts/st.sh` = headless (-display none). st.sh choisit l'EL selon l'ITS/flash0.img (EL1 standalone / EL2 si ITS avz / EL3 si flash0.img).
- [2026-06-15] Doc Sphinx (doc/source/): diagrammes générés par doc/source/img/gen_so3_diagrams.py → ./export_png.sh (CLI drawio). Après un changement CSS, rebuilder avec `make clean html` (un `make html` incrémental ne recopie pas le CSS statique).
